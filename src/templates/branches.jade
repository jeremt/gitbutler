.sk-textinput
  input(ng-model="branchName" type="text" placeholder="branch name...")
  button(ng-click="branches.create(branchName)") Create
.sk-list
  gb-branch(ng-repeat="br in data.list" name="{{br}}")
